version: "3"
services:
  node:
    image: paula/frontend-dev
    container_name: paula-dev
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "4200:4200"
    volumes:
      - src:/app/src
      - dist:/app/dist